{
  "name": " ",
  "version": "0.0.1",
  "description": "",
  "keywords": "GPL-3.0",
  "license": "",
  "author": "",
  "scripts": {
    "build": "mkdir -p dist/img && cp -rp img/* dist/img/; parcel build index.html",
    "dev": "parcel index.html --https --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
